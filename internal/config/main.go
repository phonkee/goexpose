package config

// NewFromFilename returns new config from filename
func NewFromFilename(filename string) (c *Config, err error) {
	c = &Config{}
	//if err = c.Load(filename); err != nil {
	//	return
	//}

	return
}
